import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ad7af0',
    },
    secondary: {
      main: '#db9b2c',
    },
    error: {
      main: '#e57373',
    },
    background: {
      default: '#ffffff'
    },
  },
}
);